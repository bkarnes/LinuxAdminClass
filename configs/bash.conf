# Command log — must be student-readable: verifiers grep this as the
# student user, and rsyslog's default create mode (0640 root:adm) would
# make it invisible to them. 0644 via per-action syntax overrides the
# global default.
local6.* action(type="omfile" file="/var/log/commands.log" fileCreateMode="0644")
